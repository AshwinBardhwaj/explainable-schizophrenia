# EEG Paper — Next.js Research Site

To run locally:

```bash
cd "paper_site_next"
npm install
npm run dev
```

Open http://localhost:3000

Notes:
- Images are served from `public/images/` and listed in `public/images.json`.
- Captions are editable in the browser but not persisted; edit `public/images.json` to save.
